#include<stdio.h>
int main() {
    int n = 1;
    while(n <= 10) {
        printf("%d\n", n);
        n++;
    }
    return 0;
}

/*Explaination: While loop is used to execute a block of code repeatedly as long as a specified condition is true.It's same as for loop. Just the syntax is different.*/